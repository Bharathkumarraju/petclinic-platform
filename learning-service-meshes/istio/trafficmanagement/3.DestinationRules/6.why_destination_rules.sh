Why Use a Destination Rule?

Defines subsets (e.g., v1/v2) and applies policies to control traffic across service versions

Configures Istio's load balancing to a service

Configures limits like max connections, timeouts, and idle connections to protect services from overload

Use Destination Rules to apply circuit breaking and reroute traffic during service failures

Can be configured to enforce mTLS between services

